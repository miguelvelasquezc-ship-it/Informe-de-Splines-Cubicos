#librerias instaladas numpy, matplotlib, pandas
#en la ventana cmd ejecutar: "pip install numpy" , "pip install matplotlib" , "pip install pandas"
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

#datos de frecuencia en khz
f = np.array([
    10.0,12.5,15.0,17.5,20.0,22.5,25.0,27.5,30.0,32.5,
    35.0,37.5,40.0,42.5,45.0,47.5,50.0,52.5,55.0,57.5,
    60.0,62.5,65.0,67.5,70.0,72.5,75.0,77.5,80.0,82.5,
    85.0,87.5,90.0,92.5,95.0,97.5,100.0,102.5,105.0,107.5
])

#datos de voltaje
V = np.array([
    0.842,0.911,0.986,1.062,1.143,1.227,1.314,1.401,1.482,1.551,
    1.216,1.048,0.866,0.689,0.521,0.364,0.223,0.103,0.012,-0.041,
    -0.057,-0.034,0.018,0.096,0.197,0.318,0.452,0.579,0.700,0.809,
    0.611,0.688,0.756,0.811,0.856,0.894,0.926,0.954,0.980,1.004
])

#paso entre datos consecutivos en khz
h = 2.5

#funcion de diferencia centrada de orden 2

#calcula la derivada numerica usando puntos anterior y siguiente
def diferencia_centrada_orden2(i):

    return (V[i+1] - V[i-1]) / (2*h)


#busca el indice correspondiente a 40 khz
i40 = np.where(f == 40.0)[0][0]

#busca el indice correspondiente a 70 khz
i70 = np.where(f == 70.0)[0][0]

#busca el indice correspondiente a 100 khz
i100 = np.where(f == 100.0)[0][0]


#calcula la derivada en 40 khz
dV40 = diferencia_centrada_orden2(i40)

#calcula la derivada en 70 khz
dV70 = diferencia_centrada_orden2(i70)

#calcula la derivada en 100 khz
dV100 = diferencia_centrada_orden2(i100)



#creacion de tabla de resultados

#organiza los resultados en una tabla
tabla = pd.DataFrame({
    "Frecuencia (kHz)": [40.0, 70.0, 100.0],
    "dV/df (V/kHz)": [
        f"{dV40:.6f}",
        f"{dV70:.6f}",
        f"{dV100:.6f}"
    ]
})


#tabla grafica

#crea la figura y los ejes
fig, ax = plt.subplots(figsize=(7,3))

#oculta los ejes para mostrar solo la tabla
ax.axis('off')

#crea la tabla visual
tabla_grafica = ax.table(
    cellText=tabla.values,
    colLabels=tabla.columns,
    cellLoc='center',
    loc='center'
)

tabla_grafica.auto_set_font_size(False)
tabla_grafica.set_fontsize(11)
tabla_grafica.scale(1.2, 2)


plt.title("Diferencia Centrada de Orden 2")
#muestra la tabla final
plt.show()
